// To test github commands in terminal
for (let i = 0; i < 3; i++) {
    console.log(i);
}

// Trying new method, with array
let testArray = [];
for (let i = 0; i < 10; i++) {
    testArray.push(i);
}